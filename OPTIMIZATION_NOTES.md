# 2026-09-07 优化记录

优化前完整备份：`../百家足球版-优化前备份-20260907-145504`，包含当时的未提交文件、Git 历史、数据和本地环境。恢复时先保留当前目录，再从备份复制，避免覆盖后续新增资料。

本轮保留原有业务、数据文件、路图规则与主要布局，完成：

- 路图生成消除循环内 Object.keys 全量扫描；路图和赛果列表使用文档片段批量插入，筛选结果复用。
- 赛事加载成功后才清除更新红点。
- 聊天与客服在隐藏页面暂停拉取，返回时补拉；浏览旧消息不强制滚到底部；客服拉取防重入、消息去重。
- 发送消息不推进读取游标，避免跳过并发发来的消息；聊天加载与发送失败提示。
- 赛程接口完整成功时允许数量正常下降或清空；接口报错、缺失结果、队名未映射时不视为完整抓取，仍保留数量保护。
- JSON、JS 完整序列化后用临时文件分别原子替换，避免读到半截文件。两文件之间不是跨文件事务。
- 赛程展示自身更新时间及陈旧提示；大小球统计文字兼容默认盘口。
- 后台聊天用户批量查询，过期清理最多每分钟一次；访问日志统计缓存一分钟并在线程池中运行。
- auth 下三个 HTML 改为指向根目录的相对符号链接，根目录为唯一页面来源。单独打包 auth 时必须一并携带根目录页面或解引用链接。
- 忽略本地依赖、缓存和 SQLite 数据库，未自动提交现有未跟踪资料。
- 手机端恢复固定导航栏的顶部留白，客服窗口适配可视视口和底部安全区，输入框16px，主要操作按钮至少44px，320px赛事网格适配。
- 初盘快照保留期扩至 420 天；任一联赛数据源失败时保留旧的赛果、赛程和大小球文件；后台手动更新改为工作线程执行，且 fetch 失败不再继续 reset。

验证：

```sh
python3 -m unittest discover -s tests -v
node tests/check_road.cjs
auth/.venv/bin/python tests/check_backend.py
npm run dev
```

200组路图输出与优化前结果一致；一次6600条样本算法计时约543ms→2ms（不是整页速度）。后台接口使用临时SQLite，验证消息增量、显示名不泄露邮箱、未登录权限及统计缓存。桌面与320/390像素窄屏检查了首页、赛季/赛事切换、大小球、详情及客服窗口边界。未做iOS/Android真机软键盘验证。

当前是本地优化，未发布线上，未调用生产数据抓取或修改历史比赛数据。线上后端与本地有版本差异（地区统计），部署前必须核对服务器版本，不能直接用本地旧后端覆盖。auth 原本未跟踪，版本提交时需单独审阅源码与部署资料，勿加入本地数据库/凭据。
